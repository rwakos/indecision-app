console.log("Utils");


export const square = (x) => x * x;
export const add = (a, b) => a+b;

const subtract = (a, b) => a-b;
export default subtract; //Defaults can not have const + name...

//export {square, add};